export type PromptBrief = { prompt:string; provider:string; task:string; audience:string; tone:string; output:string; depth:string; style:string; language:string; constraints:string[]; creativity:number; detail:number; structure:number };
export const defaultBrief:PromptBrief={prompt:"",provider:"None",task:"None",audience:"None",tone:"None",output:"None",depth:"None",style:"None",language:"None",constraints:[],creativity:50,detail:50,structure:50};
export function compilePrompt(b:PromptBrief){const use=(k:string,v:string)=>v!=="None"?`${k}: ${v}\n`:"";return `${use("Task",b.task)}User request: ${b.prompt||"[Enter your prompt]"}\n${use("Audience",b.audience)}${use("Tone",b.tone)}${use("Style",b.style)}${use("Language",b.language)}${use("Provider",b.provider)}${use("Output",b.output)}${use("Depth",b.depth)}Controls: creativity ${b.creativity}/100; detail ${b.detail}/100; structure ${b.structure}/100.\nConstraints: ${b.constraints.join("; ")||"None selected"}\n\nRespond according to the selected controls. Do not invent missing facts.`}
export function toSchema(b:PromptBrief){return {schema_version:"1.0",type:"prompt_brief",user_prompt:b.prompt||null,selections:{...b},completeness:{score:b.prompt.trim()?100:0,missing_fields:b.prompt.trim()?[]:["user_prompt"]}}}
