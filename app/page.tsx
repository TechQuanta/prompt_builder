"use client";
import {useMemo,useState} from "react"; import {Braces,Clipboard,FileJson,Moon,Sun} from "lucide-react"; import {compilePrompt,defaultBrief,toSchema,type PromptBrief} from "@/lib/prompt-builder";
const choices={provider:["None","ChatGPT","Claude","Gemini","Universal"],task:["None","Write","Plan","Analyze","Code","Research","Image prompt"],audience:["None","General","Beginner","Professional","Executive","Children"],tone:["None","Clear","Concise","Creative","Technical","Friendly","Persuasive"],output:["None","Markdown","JSON","Table","Checklist","Email","Step-by-step"],depth:["None","Quick","Balanced","Thorough"],style:["None","Zero-shot","Few-shot","Socratic","Chain of thought","Role-based"],language:["None","English","Hindi","Spanish","French","German"]} as const; const rules=["State assumptions","Ask before guessing","Use examples","Cite sources","Avoid jargon","Include risks","Be actionable","No markdown"];
export default function Page(){const[b,setB]=useState<PromptBrief>(defaultBrief);const[dark,setDark]=useState(true);const[copied,setCopied]=useState(false);const schema=useMemo(()=>toSchema(b),[b]);const prompt=useMemo(()=>compilePrompt(b),[b]);const set=<K extends keyof PromptBrief>(k:K,v:PromptBrief[K])=>setB(s=>({...s,[k]:v}));const copy=async()=>{await navigator.clipboard.writeText(prompt);setCopied(true);setTimeout(()=>setCopied(false),1200)};return <main className={dark?"dark":""}><div className="min-h-screen bg-[var(--background)] text-[var(--foreground)]"><header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-5"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-lg border-2 border-current text-sm font-black">P</span><div><h1 className="font-semibold tracking-tight">Prompt Builder</h1><p className="text-xs opacity-55">Build a better starting prompt</p></div></div><button aria-label="Toggle theme" onClick={()=>setDark(!dark)} className="rounded-full border border-[var(--line)] p-2">{dark?<Sun size={16}/>:<Moon size={16}/>}</button></header><div className="mx-auto max-w-6xl px-5 pb-12"><section className="mx-auto max-w-4xl pt-8 text-center sm:pt-14"><p className="text-xs font-medium uppercase tracking-[.2em] opacity-50">Prompt workspace</p><h2 className="mt-3 text-3xl font-semibold tracking-tight sm:text-5xl">Start with what you want.</h2><p className="mx-auto mt-3 max-w-xl text-sm leading-6 opacity-65">Add controls only when they help. Leave everything else untouched.</p><textarea value={b.prompt} aria-label="Your prompt" onChange={e=>set("prompt",e.target.value)} placeholder="Describe what you need help with…" className="mt-8 min-h-32 w-full rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-5 text-left text-base outline-none focus:border-current"/></section><section className="mx-auto mt-8 max-w-5xl rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-5 sm:p-7"><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(choices).map(([k,v])=><Choice key={k} label={k} values={v} selected={b[k as keyof typeof choices]} onChange={x=>set(k as keyof typeof choices,x as never)}/>)}</div><div className="mt-7 grid gap-5 border-t border-[var(--line)] pt-6 md:grid-cols-[1fr_1fr]"><div><p className="mb-2 text-sm font-medium">Extra rules</p><div className="flex flex-wrap gap-2">{rules.map(r=><button key={r} onClick={()=>set("constraints",b.constraints.includes(r)?b.constraints.filter(x=>x!==r):[...b.constraints,r])} className={`rounded-full border px-3 py-1.5 text-xs ${b.constraints.includes(r)?"bg-[var(--foreground)] text-[var(--background)]":"border-[var(--line)]"}`}>{r}</button>)}</div></div><div className="grid grid-cols-3 gap-4">{(["creativity","detail","structure"] as const).map(k=><label key={k} className="text-xs font-medium capitalize">{k}<span className="float-right opacity-50">{b[k]}</span><input className="mt-3 w-full accent-current" type="range" min="0" max="100" value={b[k]} onChange={e=>set(k,Number(e.target.value))}/></label>)}</div></div></section><section className="mt-8 grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><div className="rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-5 sm:p-7"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.16em] opacity-50">Generated prompt</p><h3 className="mt-1 text-lg font-semibold">Ready to copy</h3></div><button onClick={copy} className="flex items-center gap-2 rounded-lg border border-[var(--line)] px-3 py-2 text-sm"><Clipboard size={15}/>{copied?"Copied":"Copy"}</button></div><pre className="mt-5 min-h-56 whitespace-pre-wrap font-mono text-sm leading-6 opacity-85">{prompt}</pre></div><aside className="rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-5 sm:p-7"><div className="flex items-center gap-2"><FileJson size={17}/><p className="text-xs uppercase tracking-[.16em] opacity-50">Live JSON</p></div><pre className="mt-5 max-h-72 overflow-auto font-mono text-xs leading-5">{JSON.stringify(schema,null,2)}</pre></aside></section></div></div></main>}
function Choice({label,values,selected,onChange}:{label:string;values:readonly string[];selected:string;onChange:(v:string)=>void}){return <fieldset><legend className="mb-2 text-xs font-medium capitalize opacity-65">{label}</legend><select aria-label={label} value={selected} onChange={e=>onChange(e.target.value)} className="w-full rounded-lg border border-[var(--line)] bg-transparent px-3 py-2 text-sm outline-none">{values.map(v=><option key={v}>{v}</option>)}</select></fieldset>}
